# Automatically activate the ESP32-S3 development shell when entering this directory
use flake ..#esp32-s3

# Source ESP Rust environment if available
if [ -f "$HOME/export-esp.sh" ]; then
  source "$HOME/export-esp.sh"
fi
