apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: adguard
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: adguard
    app.kubernetes.io/service: adguard
  name: adguard
  namespace: kube-system
spec:
  ports:
    - name: http
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app.kubernetes.io/controller: adguard
    app.kubernetes.io/instance: adguard
    app.kubernetes.io/name: adguard
  type: ClusterIP
