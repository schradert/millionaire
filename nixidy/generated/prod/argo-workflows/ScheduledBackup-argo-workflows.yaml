apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: argo-workflows
  namespace: cicd
spec:
  backupOwnerReference: self
  cluster:
    name: argo-workflows
  schedule: 0 0 * * *
