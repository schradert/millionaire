apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: heretic
  namespace: ai
spec:
  data:
    - remoteRef:
        key: ai/huggingface/token
      secretKey: hf_token
      sourceRef:
        storeRef:
          kind: ClusterSecretStore
          name: bitwarden
