apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/instance: kosync
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: kosync
  name: kosync
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
