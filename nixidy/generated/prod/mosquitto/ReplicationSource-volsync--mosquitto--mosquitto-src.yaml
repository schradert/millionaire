apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: volsync--mosquitto--mosquitto-src
  namespace: home
spec:
  restic:
    copyMethod: Direct
    moverSecurityContext:
      fsGroup: 101
    pruneIntervalDays: 8
    repository: volsync--mosquitto--mosquitto
    retain:
      daily: 1
      monthly: 1
      weekly: 1
      yearly: 1
  sourcePVC: mosquitto
  trigger:
    schedule: 0 4 * * *
