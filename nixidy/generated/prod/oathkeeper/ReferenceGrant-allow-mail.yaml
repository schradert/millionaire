apiVersion: gateway.networking.k8s.io/v1
kind: ReferenceGrant
metadata:
  name: allow-mail
  namespace: identity
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: mail
  to:
    - group: ""
      kind: Service
      name: oathkeeper-proxy
