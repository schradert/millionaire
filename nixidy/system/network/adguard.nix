# AdGuard Home has been migrated to the hyena VPS (Hetzner).
# DNS is now served via headscale tailnet. See flake.nix hyena node config.
# Keeping this file (empty) so the import in default.nix doesn't break.
# Original k8s deployment preserved in git history for rollback reference.
{...}: {}
